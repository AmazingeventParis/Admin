services:
  admin-hub:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - admin-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - coolify

networks:
  coolify:
    external: true

volumes:
  admin-data:
